@model IEnumerable<WardrobeProject.Models.Shoe>

@{
    ViewBag.Title = "My Shoes";
}

<head>
    <link href="~/Content/ItemViewSheet.css" type="text/css" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css?family=Anton|Francois+One|Oswald" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Leckerli+One" rel="stylesheet">

</head>

<div class="title">
    <h1>My Shoes</h1>
    <p>
        @Html.ActionLink("Add a New Pair of Shoes", "Create")
    </p>
</div>
<table class="table">
    <tr>
        <th>
            Photo
        </th>
        <th>
            Description
        </th>
        <th>
            Occasion
        </th>
        <th>
            Color
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            <img src="~/Content/Images/Shoes/Shoes1.jpg" />
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ShoeName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ShoeType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ShoeColor)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ShoeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ShoeID })
        </td>
    </tr>
}

</table>
